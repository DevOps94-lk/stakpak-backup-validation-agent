# MySQL is installed on the host droplet, NOT in Docker
# DB_HOST must stay as host.docker.internal so containers can reach host MySQL
DB_HOST=host.docker.internal
DB_PORT=3306
DB_USER=stakpak_user
DB_PASSWORD=StrongPassword123!
DB_NAME=stakpak_agent_db
